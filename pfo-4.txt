#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin >> n;
    for (int i = n; i >= 1; i -= 2) {
        cout << i << " ";
    }
    for (int i = (n % 2 == 0 ? 1 : 2); i <= n; i += 2) {
        cout << i << " ";
    }
}